[pytest]
filterwarnings =
    ignore:unit_vertices:DeprecationWarning
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    fast: all tests that are not slow
testpaths =
    tests
generate_report_on_test = True